# Shape Library

This directory is the placeholder for offline critical-nucleus shape descriptors.

In the dual-track architecture, shape descriptors are generated by
`cnt_scan_pipeline.py` under `Results/dual_track_calibration/shape_library/`.
They are calibration artifacts only. Runtime PF evolution must not select CUDA
insertion templates from this library.

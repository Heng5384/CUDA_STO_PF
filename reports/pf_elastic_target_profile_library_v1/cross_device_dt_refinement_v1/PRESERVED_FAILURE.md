# Preserved early refinement failure

This directory is historical fail-closed evidence from an early comparison
with unequal relaxation depth and a different source-tree identity. It is
preserved for auditability and is not the selected production decision.

The final same-source cluster timestep refinement is:

```text
PASS_ELASTIC_TARGET_PROFILE_DT_AND_DEVICE_REFINEMENT_V1
```

See `../cluster_dt_refinement.md` and the frozen evidence under
`../evidence/cluster_selected_dt0p025_v16`.

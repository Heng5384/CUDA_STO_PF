# Operator-isolation result

The zero-mode qualification evidence confirms finite fields, no clipping,
no physical mass projection, and final mean mass errors below 1e-17. The raw
fixture materializer independently verifies
<h>=0.02392954476632684 and <C_B_tot>=0.03 to roundoff.

Periodic particle observation is read-only and is applied only to checkpoint
fields; it cannot alter the solver state.

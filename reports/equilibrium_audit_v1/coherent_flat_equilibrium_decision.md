# Coherent flat equilibrium decision

`BLOCKED_NUMERICAL_SLAB_INTERFACE`: source audit confirms fixed-cell periodic elasticity and finite-particle elastic runs are available, but the current branch has no supported 1-D slab initializer. The legacy `oneD_test_mode` fields are not consumed by the runtime, and zero-mode explicitly rejects raw/VTK continuation. Therefore no coherent planar solvus is claimed; finite-particle fixed-cell observations are reported separately.

# Chemical flat equilibrium decision

`PASS_ANALYTIC_CHEMICAL_FLAT_ROOT`; the runtime reaction root and pure-beta
common tangent are algebraically identical.  An isolated fresh-raw extension
also recovers the expected shrink/grow sign bracket around the root (details in
`chemical_flat_interface_extension_diagnostic.md`), but this is not clean
branch evidence.  `BLOCKED_NUMERICAL_SLAB_INTERFACE` remains because the
legacy slab key is ignored and the clean raw/VTK route is incompatible with the
frozen zero-mode provenance contract.

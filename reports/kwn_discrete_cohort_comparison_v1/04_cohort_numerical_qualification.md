# Discrete-cohort numerical qualification

Status: `PASS_DISCRETE_COHORT_NUMERICS`.  C1–C5 unit-test return code: `0`; C6 tolerance pass: `True`; C7 restart pass: `True`; C8 maximum residual: `0.000000e+00`.

The 1e-8 versus 1e-10 continuous metric gate is 0.1%; event-time absolute and relative differences are in `outputs/kwn_discrete_cohort_comparison_v1/cohort_tolerance_convergence.csv`.

# Discrete-event diagnosis

The old Eulerian six-particle sequence is explicitly `NON-AUTHORITY EULERIAN DISCRETE-FIXTURE DIAGNOSTIC`.

Primary root cause: `IMPLICIT_UPWIND_NUMERICAL_DIFFUSION`.  The 1600→3200 N gap is dominated at 48 h by `R10.5nm lower-bound numerical tail survival/leakage`: its survival is 0.263248 (1600) versus 0.285159 (3200).

The exact-cohort endpoint relation to the legacy Eulerian range is `EXACT_COHORT_OUTSIDE_EULERIAN_REFINEMENT_RANGE`.  The per-class timing table in `discrete_event_diagnosis.json` records whether leakage is advanced, delayed, or spurious for a physically surviving sharp cohort; none of those diagnostics licenses a P5 threshold change.

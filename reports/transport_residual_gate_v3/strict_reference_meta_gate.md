# Strict-reference V3 meta-gate

Existing 1x-5x strict reference: `PASS_PHYSICALLY_SCALED_SIGNED_DEFECT_GATE_V3`. All V3 magnitude and physically normalized signed metrics pass. Historical V2 sign-coherence failures remain recorded but are not V3 hard gates because strict `eta_interface` is approximately 1e-12 to 1e-11.

Runtime evidence: hard/retry status `PASS`, registered QoI `True`, max mass error `9.947598300641403e-13`, max phase KKT `1.0005563044757082e-10`, energy `True`, rollback `True`.

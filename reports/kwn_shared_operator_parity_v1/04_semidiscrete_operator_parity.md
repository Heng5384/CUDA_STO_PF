# Semi-discrete operator parity

Interior status: `PASS_SEMIDISCRETE_MOMENT_RATE_PARITY` with maximum moment-rate relative difference `6.648955e-03` after explicitly removing the physical lower/upper boundary terms.  The dynamic-matrix probe uses the same algebraic closure and therefore isolates transport resolution.

Boundary status is reported separately in `05_lower_boundary_audit.md`.

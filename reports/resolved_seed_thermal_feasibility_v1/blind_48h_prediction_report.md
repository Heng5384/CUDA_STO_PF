# Frozen-coefficient 48 h prediction

M1 was rejected because the 6 h calibration would require an unphysical coefficient.

M2 p=2 and p=4 are not computed because an authority-compatible strain proxy is unavailable.
